name: Legacy Kernel Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: echo "Legacy workflow retained from upstream SUSFS commit"
